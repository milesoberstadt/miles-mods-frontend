import { Component } from '@angular/core';
@Component({
  selector: 'my-app',
  template: `
    <navbar></navbar>
    <div class="container">
      <h1>App is running</h1>
    </div>
  `
})
export class AppComponent { 
}
